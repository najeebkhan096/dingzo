class MyUser {
  String ? uid;
  String ? email;
  String ? username;
  String ? location;
  String ? doc;
  String ? imageurl;

  MyUser({this.uid,this.email,this.username,this.location,this.doc,this.imageurl});

}
MyUser ? currentuser;
String user_id='';
String user_docid='';