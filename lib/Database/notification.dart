class SendNotification{
  final String ? title;
  final String ? description;
  final String ? image;
  final String ? item_name;
  final DateTime ? date;

  SendNotification({this.title,this.description,this.image,this.item_name,this.date});
}